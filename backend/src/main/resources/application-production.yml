server:
  port: ${PORT:8080}

spring:
  datasource:
    url: jdbc:h2:mem:todoplus;MODE=MYSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      enabled: false
